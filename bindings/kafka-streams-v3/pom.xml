<?xml version="1.0" encoding="UTF-8"?>
<!--
  The Kafka Streams adapter, Phase 1: a KeyValueStore backed by ElysiumKV with Streams' changelog
  and restore semantics untouched. The only thing that changes is where the bytes live.

  A separate module rather than part of the core binding, because kafka-streams is a heavy
  dependency that a plain key-value embedder has no reason to carry — and because the two have
  genuinely different audiences.

  **Versioned in the artifact name.** Phase 2 needs store-managed changelog offsets (KIP-1035), which
  exist only on Kafka 4.x, and the two cannot be one artifact: `StateStore` gained methods that do
  not compile against 3.x, and an application has exactly one Kafka on its classpath. So this is the
  3.x adapter and a `-v4` one will sit beside it, rather than this taking the unqualified name and
  leaving the other to be called something apologetic.

  kafka-streams is `provided`: a Streams application already has it on the classpath, and a version
  this module pinned would fight the one the application chose.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.veridia</groupId>
  <artifactId>elysiumkv-kafka-streams-v3</artifactId>
  <version>0.0.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>ElysiumKV Kafka Streams 3.x adapter</name>
  <description>
    A Kafka Streams state store backed by ElysiumKV, so state can live on tiered storage instead of
    local disk alone. Built against the Kafka 3.x StateStore interface.
  </description>
  <url>https://github.com/veridia-io/elysiumkv</url>

  <licenses>
    <license>
      <name>MIT License</name>
      <url>https://opensource.org/licenses/MIT</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <developers>
    <developer>
      <id>veridia</id>
      <name>Veridia</name>
      <email>oss@veridia.io</email>
      <organization>Veridia</organization>
      <organizationUrl>https://github.com/veridia-io</organizationUrl>
    </developer>
  </developers>

  <scm>
    <url>https://github.com/veridia-io/elysiumkv</url>
    <connection>scm:git:git://github.com/veridia-io/elysiumkv.git</connection>
    <developerConnection>scm:git:ssh://git@github.com/veridia-io/elysiumkv.git</developerConnection>
  </scm>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>11</maven.compiler.release>
    <!-- The engine this adapter is built against. A property rather than a literal so the release
         can point it at the version being published, from the same tag that names both. Adapter
         and engine are versioned together: they share a repository and a release. -->
    <elysiumkv.version>0.0.0-SNAPSHOT</elysiumkv.version>
    <kafka.version>3.9.1</kafka.version>
    <junit.version>5.13.4</junit.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>io.veridia</groupId>
      <artifactId>elysiumkv</artifactId>
      <version>${elysiumkv.version}</version>
    </dependency>

    <dependency>
      <groupId>org.apache.kafka</groupId>
      <artifactId>kafka-streams</artifactId>
      <version>${kafka.version}</version>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>org.apache.kafka</groupId>
      <artifactId>kafka-streams-test-utils</artifactId>
      <version>${kafka.version}</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <version>3.3.1</version>
        <executions>
          <execution>
            <id>attach-sources</id>
            <goals><goal>jar</goal></goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
        <version>3.11.2</version>
        <executions>
          <execution>
            <id>attach-javadocs</id>
            <goals><goal>jar</goal></goals>
          </execution>
        </executions>
        <configuration>
          <!-- Same reasoning as the engine binding: the docs here are prose about behaviour, and
               doclint would fail the build over missing @param tags on self-evident setters. -->
          <additionalJOptions><additionalJOption>-Xdoclint:none</additionalJOption></additionalJOptions>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.sonatype.central</groupId>
        <artifactId>central-publishing-maven-plugin</artifactId>
        <version>0.9.0</version>
        <extensions>true</extensions>
        <configuration>
          <publishingServerId>central</publishingServerId>
          <!-- Publish without a manual step in the Central UI, and do not report success until
               Central says it is published: a deploy that returns while validation is still
               pending would let a failed release look green. -->
          <autoPublish>true</autoPublish>
          <waitUntil>published</waitUntil>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>release</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-gpg-plugin</artifactId>
            <version>3.2.7</version>
            <executions>
              <execution>
                <id>sign-artifacts</id>
                <phase>verify</phase>
                <goals><goal>sign</goal></goals>
              </execution>
            </executions>
            <configuration>
              <!-- Loopback pinentry with no agent: a runner has no interactive terminal, and the
                   passphrase arrives through MAVEN_GPG_PASSPHRASE. -->
              <useAgent>false</useAgent>
              <gpgArguments>
                <arg>--batch</arg>
                <arg>--pinentry-mode</arg>
                <arg>loopback</arg>
              </gpgArguments>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
